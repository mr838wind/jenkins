version: '3.4'
services:
  jenkins:
    image: mr838wind/jenkins_with_plugins:latest
    ports:
      - "82:8080"
    volumes:
      - jenkins_home:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock
#  ssh-agent:
#    image: jenkins/ssh-agent
volumes:
  jenkins_home: